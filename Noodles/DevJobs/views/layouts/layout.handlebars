{{!--
    Sintaxis de HTML. Las variables se tienen que encerrar entre dos brackets y 
    los bloques de contenido en tres brackets.
--}}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <title>{{nombrePagina}}</title> {{!-- El nombre de página viene desde el controlador --}}
</head>
<body>

    <div class="nombre-sitio contenedor">
        <h1>
            <a href="/">devJobs</a> {{!-- Envia hacia la página principal --}}
        </h1>
        {{#if barra}} <!-- No hay nada que explicar, es como en JavaScript, para verificar si una variable existe o no. -->
            <div class="buscador">
                <form action="/buscador" method="POST">
                    <input type="text" class="buscar" name="q">
                    <input type="submit" value="Buscar">
                </form>
            </div>
        {{/if}}
        {{#if cerrarSesion}}
            <div class="cerrar-sesion">
                <p>Hola {{nombre}}</p>
                {{#if imagen}}
                <img src="/uploads/perfiles/{{imagen}}" alt="Imagen de perfil" class="admin-perfil">
                {{/if}}
                <a href="/administracion">Administración</a>
                <a href="/cerrar-sesion">Cerrar sesión</a>
            </div>
        {{/if}}
    </div>

    <header class="site-header contenedor separador">
        <h2>{{nombrePagina}}</h2>
        {{#if tagline}}
            <p class="tagline">{{tagline}}</p>
        {{/if}}
        {{#if boton}}
            <a href="/vacantes/nueva" class="btn btn-azul">Publicar nueva vacante</a>
        {{/if}}
    </header>

    <div class="contenido-principal contenedor">
        {{#if mensajes}}
            <div class="alertas">
                {{#mostrarAlertas mensajes}}
                {{/mostrarAlertas}}
            </div>
        {{/if}}
        {{{body}}} 
        <!-- Lo que tengamos en los demás templates se va a heredar aquí.
        Eso es debido a que en nuestro index ya indicamos que se realize de manera automática en este archivo 
        de layout. Por eso se definió como el principal. -->
    </div>

    <script src="/js/trix.js"></script>
    <script src="/dist/bundle.js"></script>
</body>
</html>